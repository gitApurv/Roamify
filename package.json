{
  "name": "vite-node-fullstack-app",
  "version": "1.0.0",
  "scripts": {
    "install-client": "cd client && npm install",
    "build": "cd client && npm run build",
    "start": "node server/index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3"
  }
}
